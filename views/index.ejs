<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <form action="/" method="GET">
      <label for="search">Search username: </label>
      <input type="text" id="search" name="search" value="Damon" />
      <button type="submit">Search</button>
    </form>
    <div>
      <% usernames.map(user => { %>
      <div><%= user.username %></div>
      <% }) %>
    </div>
    <a href="/delete">Delete ALL</a>
  </body>
</html>
